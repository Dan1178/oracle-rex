staticfiles

# SQLite WAL sidecar files (transient; created when DB runs in WAL mode)
db.sqlite3-wal
db.sqlite3-shm

# Python
__pycache__/
*.py[cod]

# Virtual environments
.venv/
venv/
env/

# OS / editor
.DS_Store
.idea/
.vscode/

# Stray root npm lockfile (the real one lives in frontend/)
/package-lock.json
